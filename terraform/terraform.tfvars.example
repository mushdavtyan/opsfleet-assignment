region       = "us-east-1"
cluster_name = "eks-karpenter"

# Restrict the EKS API to your IP / VPN in any shared account:
# cluster_endpoint_public_access_cidrs = ["203.0.113.10/32"]

# Optional: static keys instead of AWS_PROFILE / SSO
# access_key = "AKIA..."
# secret_key = "..."
# token      = "..."
# profile    = "my-profile"
